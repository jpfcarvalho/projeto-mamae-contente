spring:
  application:
    name: Mamae Contente
  datasource:
    url: jdbc:postgresql://localhost:5432/mamaecontente
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration